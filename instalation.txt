#! /bin/bash
#tester le nom de la machine
hostname="rick"
hostname2=$(cat /etc/hostname)
if [ $hostname = $hostname2 ]
	then
		echo "il est rick"
	else

		echo "rick" > /etc/hostname
		echo "127.0.1.1 rick" > /etc/hosts
fi
/etc/init.d/hostname.sh
#tester utilisateur carole
grep carole /etc/password /etc/group
if [ $user = "carole" ]
	then
		echo "oui je suis carole "
	else
	
		adduser "carole"
		echo -e "carole:daryl" | chpasswd
fi
apt-get install sudo
echo -e "sudo carole ALL=ALL ALL" >> /etc/suders

